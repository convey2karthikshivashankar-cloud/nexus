import { Metrics, Event } from '../App';
interface Props {
    metrics: Metrics;
    events: Event[];
}
export default function PerformanceMetrics({ metrics, events }: Props): import("react").JSX.Element;
export {};
